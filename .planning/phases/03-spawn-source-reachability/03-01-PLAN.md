---
phase: 03-spawn-source-reachability
type: execute
domain: unity-static-ecs
---

<objective>
Implement bounded spawn-source reachability state.

Purpose: let director source selection use cached navigation facts instead of doing pathfinding every tick.
Output: `SpawnSourceReachabilitySystem` updates `SpawnSourceNavState` under a per-tick performance budget.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/02-runtime-nav-areas/02-02-SUMMARY.md
@ai/CombatDirector_AiNavigation_Contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reachability scheduling rules</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Domain/SpawnSourceReachabilityRules.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/SpawnSourceReachabilityRequest.cs</files>
  <action>Add explicit domain rules that decide when a source should be checked again based on status, nav version, last check time, next check time, and per-cell budget. Use a request component or event only if needed by the existing ECS style discovered during implementation. Do not let `CombatDirector` run path checks directly.</action>
  <verify>Search director-side systems for direct pathfinding calls; none should be introduced.</verify>
  <done>Reachability scheduling is deterministic and bounded by navigation-owned rules.</done>
</task>

<task type="auto">
  <name>Task 2: Update SpawnSourceNavState from bounded checks</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/SpawnSourceReachabilitySystem.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/AiNavigationLogicFeature.cs</files>
  <action>Add a server system that reads director-owned `SpawnSource`, reads active `CombatCellNavArea`, consumes no more than `MaxReachabilityChecksPerTick`, and writes only `SpawnSourceNavState`/navigation-owned counters. Status transitions must be explicit: `WaitingForNavMesh`, `Reachable`, `Unreachable`, `TemporarilyBlocked`. Do not mutate `SpawnSource.IsActive`, director phase, or threat budget.</action>
  <verify>Search the system for mutations to `SpawnSource`, `DirectorPhase`, and `ThreatBudget`; there must be none.</verify>
  <done>Reachability state updates are cached, version-aware, and budgeted.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Reachability checks per tick cannot exceed `CombatCellPerformanceBudget.MaxReachabilityChecksPerTick`.
- [ ] `SpawnSourceNavState.NavVersion` updates when nav area version changes.
- [ ] `Peak` uses cached states and does not recalculate all candidates.
- [ ] Ask the user to run Unity compile checks if MCP validation is unavailable.
</verification>

<success_criteria>
- Spawn sources have navigation-owned reachability state.
- Director can read cached status without owning pathfinding.
</success_criteria>

<output>
After completion, create `.planning/phases/03-spawn-source-reachability/03-01-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 03-02-PLAN.md`.
</output>

