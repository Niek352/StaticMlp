---
phase: 03-spawn-source-reachability
type: execute
domain: unity-static-ecs
---

<objective>
Expose cached spawn candidates and resolved spawn points.

Purpose: keep `SpawnSourceSelectionSystem` and `SpawnRequestBuildSystem` cheap and deterministic.
Output: systems/contracts that provide reachable candidate facts and optional resolved spawn point data from cached navigation state.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/03-spawn-source-reachability/03-01-SUMMARY.md
@ai/CombatDirector_AiNavigation_Contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build reachable candidate cache</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/ReachableSpawnSourceCandidate.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/ReachableSpawnSourceCacheSystem.cs</files>
  <action>Add a navigation-owned candidate component or event that marks active reachable sources for director consumption. The cache must derive only from `SpawnSource.IsActive`, `SpawnSourceNavState.Status == Reachable`, active nav area membership, budget state, safe/min/max distance facts if those already exist, and nav version. Do not score dramaturgy here; director still owns wave selection and final spawn choice.</action>
  <verify>Inspect candidate system and confirm it does not read or write `ThreatBudget` and does not decide wave type/enemy composition.</verify>
  <done>Director can query reachable source candidates without triggering pathfinding.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare resolved spawn points from cached nav data</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/ResolvedSpawnPointSystem.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/ResolvedSpawnPoint.cs</files>
  <action>Add a server system that writes `ResolvedSpawnPoint` for sources with valid cached reachability and nav version. Use `EntityGID Source`, spawn position, zone id, and nav version. `SpawnRequestBuildSystem` may later read this fact; it must not directly search NavMesh. If source-point projection cannot be implemented without a runtime nav backend yet, fail fast in the backend boundary instead of silently using raw source position.</action>
  <verify>Search for direct NavMesh point search in `SpawnRequestBuildSystem` or director-side build systems; none should be added.</verify>
  <done>Resolved spawn point data exists as cached navigation output, not as director-owned pathfinding.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `SpawnSourceSelection` can be implemented using cached reachable candidates only.
- [ ] `SpawnRequestBuildSystem` has a cached `ResolvedSpawnPoint` path and no direct NavMesh search.
- [ ] Navigation does not choose director phase, wave type, enemy composition, loot, or rewards.
- [ ] Ask the user to run Unity compile checks if MCP validation is unavailable.
</verification>

<success_criteria>
- Reachable spawn candidates and resolved points are explicit navigation outputs.
- Director remains the owner of spawn decision and request creation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-spawn-source-reachability/03-02-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 04-01-PLAN.md`.
</output>

