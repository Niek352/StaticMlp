---
phase: 02-runtime-nav-areas
type: execute
domain: unity-static-ecs
---

<objective>
Add runtime nav build queue, versioning, and budget counters.

Purpose: keep expensive nav work outside `CombatDirector` and make phase-sensitive throttling possible.
Output: navigation-owned queue/state components and systems that process bounded rebuild work.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/02-runtime-nav-areas/02-01-SUMMARY.md
@ai/CombatDirector_AiNavigation_Contract.md
@Assets/Scripts/StaticMlp/Features/AiBots/Runtime/Navigation/AiNavigation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nav queue and version state</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/RuntimeNavMeshZoneState.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/NavRebuildRequest.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/NavWorkBudgetCounter.cs</files>
  <action>Add data-only components for nav zone state, queued rebuild request, and per-tick technical counters. Include nav version, build state, next allowed rebuild time, and counted operations such as path requests and reachability checks. Do not put director phase or threat budget into these components; phase is an input read by scheduling systems, not navigation-owned state.</action>
  <verify>Inspect contracts and confirm no threat/dramaturgy fields exist in navigation-owned budget counters.</verify>
  <done>Runtime nav work can be represented without depending on CombatDirector internals.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded rebuild scheduling</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/RuntimeNavMeshRebuildQueueSystem.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/RuntimeNavMeshBuildSystem.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/AiNavigationLogicFeature.cs</files>
  <action>Add systems that enqueue and process rebuild requests from navigation areas within `CombatCellPerformanceBudget`. During `Peak`, the build system must not start mass rebuilds; it may process only explicitly allowed/prewarmed work. If the actual NavMesh building implementation is not ready, define the ECS queue/version contract and mark backend execution as not implemented by throwing a clear exception when invoked, not by silently succeeding.</action>
  <verify>Search for silent fallbacks around missing runtime nav backend; invalid/missing backend must throw.</verify>
  <done>Nav rebuild requests are budgeted, versioned, and cannot be started unbounded during `Peak`.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `CombatDirector` contains no `NavMeshBuilder`, `NavMeshData`, or direct runtime NavMesh build calls.
- [ ] `Peak` path cannot trigger mass rebuild work.
- [ ] Technical counters are separate from `ThreatBudget`.
- [ ] Ask the user to run Unity compile checks if MCP validation is unavailable.
</verification>

<success_criteria>
- Runtime nav work has explicit queue, version, and budget state.
- Expensive work can be throttled without hiding failures.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-nav-areas/02-02-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 03-01-PLAN.md`.
</output>

