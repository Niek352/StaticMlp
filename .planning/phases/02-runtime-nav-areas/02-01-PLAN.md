---
phase: 02-runtime-nav-areas
type: execute
domain: unity-static-ecs
---

<objective>
Sync combat cells into owned navigation areas.

Purpose: let `CombatDirector` describe where combat happens while `AiNavigation` owns nav area state and build/prewarm parameters.
Output: `CombatCellNavAreaSyncSystem` that reads `CombatCell` and owns `CombatCellNavArea`.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-ainavigation-architecture/01-02-SUMMARY.md
@ai/CombatDirector_AiNavigation_Contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nav area sync system</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/CombatCellNavAreaSyncSystem.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/AiNavigationLogicFeature.cs</files>
  <action>Add a server system that reads `CombatCell` from the owning director contracts assembly and creates/updates `CombatCellNavArea` on the same entity or a clearly owned nav-area entity, depending on existing director data shape discovered during implementation. `AiNavigation` may read `CombatCell`, but it must not mutate `CombatCell`, director phase, or threat budget. Use direct `Read`/`Mut` for guaranteed state and fail fast for invalid architecture; do not add broad `Ensure*` helpers.</action>
  <verify>Search the system for mutations to `CombatCell`, `DirectorPhase`, and `ThreatBudget`; there must be none.</verify>
  <done>Each active combat cell has corresponding navigation area state with build radius, source collect radius, and priority derived by explicit rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic area parameter rules</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Domain/CombatCellNavAreaRules.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/CombatCellNavAreaSyncSystem.cs</files>
  <action>Add a small explicit `Domain/Rules` type for deriving `NavBuildRadius`, `SourceCollectRadius`, and `Priority` from `CombatCell`. Keep it pure and deterministic. Do not create a generic helper/utility bucket. Validate only rule inputs that are external or configuration-like; invalid internal combat cell geometry should fail fast.</action>
  <verify>Inspect that rules live under `Domain` or `Domain/Rules`, are deterministic, and are not named `Helper` or `Utility`.</verify>
  <done>Area parameters are centralized in an explicit rules type and system logic stays focused on ECS synchronization.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `AiNavigation` reads `CombatCell` but does not own or mutate it.
- [ ] No `UnityEngine.AI` or `NavMeshBuilder` usage was introduced into director code.
- [ ] System order places nav area sync before nav rebuild/reachability and before director source scoring.
- [ ] Ask the user to run Unity compile checks if MCP validation is unavailable.
</verification>

<success_criteria>
- Combat areas are represented as `CombatCellNavArea`.
- Ownership is clear: director owns combat cell facts; navigation owns nav area runtime state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-nav-areas/02-01-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 02-02-PLAN.md`.
</output>

