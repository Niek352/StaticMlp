---
phase: 01-ainavigation-architecture
type: execute
domain: unity-static-ecs
---

<objective>
Create the standalone `StaticMlp.Features.AiNavigation` contract surface.

Purpose: prevent `AiBots` from becoming a broad cross-feature navigation platform while preserving the existing ProjectDawn-backed local bot movement path.
Output: contracts-only asmdef and one-type-per-file ECS contracts for nav areas, spawn source nav state, performance budgets, resolved spawn points, and far AI state.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@Assets/Scripts/StaticMlp/Features/AiBots/Runtime/Navigation/AiNavigation.md
@ai/CombatDirector_AiNavigation_Contract.md
@Assets/Scripts/StaticMlp/Features/AiBots/AGENTS.md
@ai/ECS_Feature_Architecture_Layout_StaticEcs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AiNavigation contracts assembly</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/StaticMlp.Features.AiNavigation.Contracts.asmdef</files>
  <action>Create a contracts-only asmdef named `StaticMlp.Features.AiNavigation.Contracts` with references only to `FFS.StaticEcs`, `Game.Core`, and Unity math/engine assemblies already used by nearby contracts. Do not reference `ProjectDawn.Navigation`, `Unity.Entities`, `StaticMlp.Features.AiBots`, `StaticMlp.Features.Combat.Logic`, or presentation assemblies. The contracts assembly is the public boundary that `CombatDirector`, `AiBots`, and base NPC logic may read.</action>
  <verify>Inspect the asmdef JSON and confirm it has no ProjectDawn, Unity.Entities backend, AiBots, Combat.Logic, or Presentation references.</verify>
  <done>Contracts asmdef exists and dependency direction is from specialized gameplay features toward shared contracts, not from shared navigation toward specialized gameplay.</done>
</task>

<task type="auto">
  <name>Task 2: Add public navigation contract types</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/CombatCellNavArea.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/SpawnSourceNavStatus.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/SpawnSourceNavState.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/ResolvedSpawnPoint.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/CombatCellPerformanceBudget.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/AiFarSimulationState.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/AiNavigationMode.cs</files>
  <action>Add exactly one top-level type per file. Use `IComponent` for ECS state. Use `EntityGID` for persistent entity references; do not expose raw entity ids or store `Entity`. Keep contracts as data-only structs/enums with no gameplay methods. `ResolvedSpawnPoint` should identify its source with `EntityGID Source`, not `int SourceEntityId`, because raw entity ids are forbidden in gameplay contracts.</action>
  <verify>Run Unity MCP `validate_script` on each new C# file if Unity is available; otherwise inspect files for one top-level type per file and no `.Raw`/raw entity id fields.</verify>
  <done>All public contract types from the combat/navigation contract exist, are data-only, and use stable entity references where references are required.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] No new contract file references `ProjectDawn.Navigation`, `Unity.Entities`, `UnityEngine.AI`, or presentation namespaces.
- [ ] `ResolvedSpawnPoint` uses `EntityGID` for source identity.
- [ ] No generated replication file was edited.
- [ ] Ask the user to run Unity compile checks if MCP validation is unavailable.
</verification>

<success_criteria>
- `AiNavigation.Contracts` is consumable without backend dependencies.
- Contract types match `CombatDirector_AiNavigation_Contract.md` while respecting project rules about `EntityGID`.
- Existing `AiBots/Runtime/Navigation` code is untouched except for future consumer references.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ainavigation-architecture/01-01-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 01-02-PLAN.md`.
</output>

