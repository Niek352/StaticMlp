---
phase: 01-ainavigation-architecture
type: execute
domain: unity-static-ecs
---

<objective>
Add the minimal `AiNavigation` logic feature shell and registration path.

Purpose: make navigation ownership explicit before implementing runtime systems.
Output: logic asmdef, feature entry point, type registration for contracts, and documented system-order slots.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-ainavigation-architecture/01-01-SUMMARY.md
@Assets/Scripts/StaticMlp/Game/Bootstrap/GameplayFeature.cs
@Assets/Scripts/StaticMlp/Features/AiBots/Runtime/AiBotsGameplayFeature.cs
@ai/CombatDirector_AiNavigation_Contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AiNavigation logic feature shell</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/StaticMlp.Features.AiNavigation.Logic.asmdef, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/AiNavigationLogicFeature.cs</files>
  <action>Create a logic asmdef that references `StaticMlp.Features.AiNavigation.Contracts`, `FFS.StaticEcs`, `Game.Core`, and only the minimum Unity runtime assemblies needed for server logic. Add `AiNavigationLogicFeature : GameplayFeature` with registration methods matching the local `GameplayFeature` pattern. Keep it free of gameplay computation except type/resource/system registration. Do not include ProjectDawn backend code here.</action>
  <verify>Inspect asmdef references and feature class body; confirm feature class only registers types/resources/systems.</verify>
  <done>`AiNavigationLogicFeature` exists and is a composition entry point, not a logic bucket.</done>
</task>

<task type="auto">
  <name>Task 2: Register AiNavigation types and pipeline slots</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/AiNavigationLogicFeature.cs</files>
  <action>Register every `AiNavigation.Contracts` component type between world create and initialize using the existing project registration style. Add empty/commented server system slots only if the project pattern requires explicit ordering placeholders; otherwise register no systems until Phase 02. The intended order is after combat cell tracking/threat input and before director phase/source scoring. Do not edit `MultiplayerSystemBootstrap`; add the feature through the existing feature composition mechanism only.</action>
  <verify>Search for `new AiNavigationLogicFeature` or equivalent feature registration and confirm no direct bootstrap system insertion was added.</verify>
  <done>All new component types have a clear registration path and the plan preserves feature-based composition.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] No systems were added directly to `MultiplayerSystemBootstrap`.
- [ ] `AiNavigationLogicFeature` does not mutate ECS state.
- [ ] No ProjectDawn/backend dependency leaked into `Logic`.
- [ ] Ask the user to run Unity compile checks if MCP validation is unavailable.
</verification>

<success_criteria>
- `AiNavigation` can be composed as a normal gameplay feature.
- Contracts and logic are separated.
- Existing `AiBots` local navigation remains the only local movement application path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ainavigation-architecture/01-02-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 02-01-PLAN.md`.
</output>

