---
phase: 02-resource-flow-economy
plan: 02-09
type: execute
domain: unity-static-ecs-ui-mvc
---

<objective>
Add the first Phase 2 economy UX pass: make Workbench production state readable and actionable in the existing building management panel.

Purpose: Phase 2 added server-authoritative production, fuel, output claiming, and blocked reasons, but the player-facing Workbench panel still reads like dense debug text and does not surface the replicated blocked reason. The first UX pass should explain why production is idle and what resource/action is needed next, without changing gameplay authority or adding recipe switching before the server request path exists.
Output: Source changes that improve Workbench panel presentation state and rendering, plus documentation of the remaining Unity prefab wiring/manual verification.
</objective>

<execution_context>
@.agents/skills/create-plans/workflows/execute-phase.md
@.agents/skills/create-plans/templates/summary.md
@.agents/skills/staticmlp-code-writing/SKILL.md
@.agents/skills/staticmlp-ui-mvc/SKILL.md
@AGENTS.md
</execution_context>

<context>
@.planning/phases/02-resource-flow-economy/02-05-SUMMARY.md
@.planning/phases/02-resource-flow-economy/02-06-SUMMARY.md
@.planning/phases/02-resource-flow-economy/02-07-SUMMARY.md
@.planning/phases/02-resource-flow-economy/02-08-FINDINGS.md
@docs/ux/settlement-stage1.md
@Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/BuildingPanelPresentation.cs
@Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/BuildingManagementPanelView.cs
@Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/Definitions/WorkbenchPanelState.cs
@Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/Definitions/ProductionResourceBufferEntry.cs
@Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Logic/Components/ProductionStationOperationState.cs
@Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Logic/Definitions/ProductionStationBlockedReason.cs
@Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Logic/Catalogs/ProductionRecipeCatalog.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface production blocked reason in Workbench panel state</name>
  <files>Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/Definitions/WorkbenchPanelState.cs, Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/BuildingPanelPresentation.cs</files>
  <action>Add a presentation field that carries `ProductionStationBlockedReason` from projected `ProductionStationOperationState.BlockedReason` into `WorkbenchPanelState`. Keep this as client-only presentation state. Do not mutate `ProductionStationOperationState` from presentation, do not add fallback/default probing, and do not invent local gameplay validation. The server already owns blocked-state truth.</action>
  <verify>`rg "BlockedReason" Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation -g "*.cs"` shows the Workbench state and builder read path.</verify>
  <done>Workbench panel state contains the replicated blocked reason, populated from client projection during `BuildWorkbench`.</done>
</task>

<task type="auto">
  <name>Task 2: Render actionable Workbench production status</name>
  <files>Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/BuildingManagementPanelView.cs</files>
  <action>Replace the Workbench summary's raw status block with a clearer text model in the existing view: recipe name, progress percentage, worker count, output capacity, input rows, output rows, and a direct status line derived from blocked reason. The status line should map `MissingFuel`, `MissingInputs`, `FullOutputBuffer`, and `NoWorkers` to concise player-facing messages. Keep the current single-view architecture and existing inspector references; do not add new serialized fields, scene searches, child lookups, prefab edits, or new UI assets in this plan.</action>
  <verify>`rg "Missing fuel|Missing inputs|Output buffer|No workers|Production blocked" Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/BuildingManagementPanelView.cs` finds the player-facing mapping.</verify>
  <done>The Workbench panel tells the player whether production is running, waiting for fuel/input/workers, or blocked by full output, using existing labels/buttons.</done>
</task>

<task type="auto">
  <name>Task 3: Document first-pass UX status and remaining gaps</name>
  <files>docs/ux/settlement-stage1.md, .planning/phases/02-resource-flow-economy/02-09-SUMMARY.md</files>
  <action>Update the UX status document with a focused Phase 2 Workbench section: what the player sees now, what actions exist (`Assign Worker`, `Claim Output`), which blocked states are surfaced, and what is deliberately not included yet (`Set Recipe`, barter, prefab layout polish). Preserve architecture notes that UI forwards intent only and does not own gameplay mutation. If the existing document appears mojibake in shell output, keep edits small and do not rewrite the whole file unless encoding is explicitly fixed as a separate task.</action>
  <verify>`rg "Phase 2 Workbench|MissingFuel|Claim Output|Set Recipe|barter" docs/ux/settlement-stage1.md .planning/phases/02-resource-flow-economy/02-09-SUMMARY.md` finds the new UX status and deferrals.</verify>
  <done>Documentation matches the implemented first-pass UX and names the remaining UX gaps without implying unsupported runtime exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Workbench production status is clearer in the existing building management panel, using current UI bindings and replicated production blocked state.</what-built>
  <how-to-verify>
    1. In Unity, run the normal host/client play verification path for the settlement vertical slice.
    2. Build or use a completed Workbench.
    3. Check these scenarios: no worker assigned, missing fuel, missing inputs, output buffer full, and claimable output.
    4. Confirm the Workbench panel text explains the current state and the primary/secondary buttons remain wired to worker assignment and output claiming.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe the UI issue to fix.</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `rg "BlockedReason" Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation -g "*.cs"` confirms blocked reason reaches presentation.
- [ ] `rg "Missing fuel|Missing inputs|No workers|Output buffer" Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/BuildingManagementPanelView.cs` confirms player-facing blocked-state messages exist.
- [ ] `git diff --check -- Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/Definitions/WorkbenchPanelState.cs Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/BuildingPanelPresentation.cs Assets/Scripts/StaticMlp/Features/Settlement/Runtime/Presentation/BuildingManagementPanelView.cs docs/ux/settlement-stage1.md .planning/phases/02-resource-flow-economy/02-09-SUMMARY.md` passes, allowing only pre-existing line-ending warnings.
- [ ] Ask the user to run Unity compile/play verification; do not run `dotnet build` or Unity batchmode.
</verification>

<success_criteria>
- Workbench panel communicates production progress, input/fuel/output buffers, worker count, and blocked reason.
- UI changes remain presentation-only and do not mutate server-authoritative gameplay state.
- No prefab assets, prefab YAML, scene hierarchy changes, or Unity batchmode operations are created by the agent.
- Recipe switching and barter remain explicitly deferred because their runtime request/ownership paths are not part of this plan.
- Summary documents completed UX work, verification results, and remaining gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/02-resource-flow-economy/02-09-SUMMARY.md`:

# Phase 2 Plan 9: Workbench Production UX Summary

**[Substantive one-liner describing the Workbench UX improvement]**

## Accomplishments
- [Key UX outcome]
- [Presentation/state boundary outcome]
- [Documentation outcome]

## Files Created/Modified
- `path/to/file.cs` - What changed
- `docs/ux/settlement-stage1.md` - UX status update

## Decisions Made
[Key decisions and rationale, especially any deferrals]

## Deviations from Plan
[If no deviations: "None - plan executed exactly as written"]

## Issues Encountered
[Problems and how they were resolved, or "None"]

## Next Phase Readiness
[What UX gap should be planned next, likely recipe selection request path or BuildingMenu unlock/locked-state UX]

---
*Phase: 02-resource-flow-economy*
*Completed: [date]*
</output>
