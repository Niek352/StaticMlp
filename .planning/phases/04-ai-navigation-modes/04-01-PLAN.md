---
phase: 04-ai-navigation-modes
type: execute
domain: unity-static-ecs
---

<objective>
Introduce far/local AI navigation mode state.

Purpose: allow server-authoritative logical movement without requiring loaded local chunks or active client views.
Output: navigation-owned mode state plus a bounded far simulation system that updates logical position under a sparse tick schedule.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/03-spawn-source-reachability/03-02-SUMMARY.md
@Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/AiNavigationMode.cs
@Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/AiFarSimulationState.cs
@Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/AiNavigationLogicFeature.cs
@Assets/Scripts/StaticMlp/Features/AiBots/Runtime/Navigation/AiNavigation.md
@ai/CombatDirector_AiNavigation_Contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigation-owned mode state</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/AiNavigationModeState.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/AiFarSimulationState.cs</files>
  <action>Add a server-owned state component that uses the existing `AiNavigationMode` enum to represent current mode and next desired mode if transition intent is needed. Keep the data contract-only and navigation-owned. Reuse the existing `AiFarSimulationState` file if it needs small field adjustments, but do not create duplicate mode/far contracts or store `Entity`, GameObject, MonoBehaviour, backend handles, or view references.</action>
  <verify>Inspect contracts and confirm they contain only stable data fields and no `Entity`, GameObject, MonoBehaviour, View, or presentation fields.</verify>
  <done>AI navigation mode and far simulation state exist as navigation-owned ECS data that can survive without client view or loaded chunk.</done>
</task>

<task type="auto">
  <name>Task 2: Implement approximate far movement</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/FarAiApproximateMoveSystem.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Domain/FarAiMovementRules.cs</files>
  <action>Add a bounded server system that advances `AiFarSimulationState.LogicalPosition` toward `TargetPosition` only when `AiNavigationModeState` is in the far simulation path. Put step sizing, arrival thresholds, and next-tick scheduling in explicit domain rules. The system may update navigation-owned logical state only; it must not mutate `CharacterNetState` directly if local bot movement owns that projection. Later handoff will reconcile through `AiMoveRequest` or owner-specific events.</action>
  <verify>Search `FarAiApproximateMoveSystem` for `Mut<CharacterNetState>` and `ProjectDawn`; neither should exist.</verify>
  <done>Far AI can progress logically on sparse server ticks without local nav mesh or client view.</done>
</task>

<task type="auto">
  <name>Task 3: Register far simulation into the navigation feature</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/AiNavigationLogicFeature.cs</files>
  <action>Register the new far simulation system through `AiNavigationLogicFeature` in a server order that keeps it inside navigation-owned gameplay, after supporting nav state is available and before any later handoff logic from `04-02`. Do not patch unrelated bootstraps or place the system in `AiBots`.</action>
  <verify>Inspect `AiNavigationLogicFeature` and confirm the new system is registered there and `MultiplayerSystemBootstrap` remains untouched.</verify>
  <done>Far simulation runs as part of the standalone `AiNavigation` gameplay feature.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `AiNavigationModeState` is navigation-owned server state and not a presentation hint.
- [ ] Far movement does not require loaded chunk or active View.
- [ ] Far movement does not mutate local movement replication state directly.
- [ ] AI mode state is server-authoritative.
- [ ] The new server system is registered via `GameplayFeature`, not ad hoc bootstrap edits.
- [ ] Ask the user to run Unity compile checks if MCP validation is unavailable.
</verification>

<success_criteria>
- Far simulation is represented separately from local ProjectDawn movement.
- Mode ownership is explicit in `AiNavigation`, not inferred from presentation/runtime backend state.
- No presentation or transport concerns leak into navigation logic.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-navigation-modes/04-01-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 04-02-PLAN.md`.
</output>
