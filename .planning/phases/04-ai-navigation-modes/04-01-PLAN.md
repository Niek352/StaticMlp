---
phase: 04-ai-navigation-modes
type: execute
domain: unity-static-ecs
---

<objective>
Introduce far/local AI navigation mode state.

Purpose: allow server-authoritative logical movement without requiring loaded local chunks or active client views.
Output: mode contracts and far simulation system that updates logical position under a bounded tick schedule.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/03-spawn-source-reachability/03-02-SUMMARY.md
@Assets/Scripts/StaticMlp/Features/AiBots/Runtime/Navigation/AiNavigation.md
@ai/CombatDirector_AiNavigation_Contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mode ownership contracts</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/AiNavigationMode.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/AiNavigationModeState.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/AiFarSimulationState.cs</files>
  <action>Represent local-vs-far movement as navigation-owned state. Keep it server-authoritative and data-only. The mode contract must not be a presentation hint. Include current mode, target mode if needed, logical position, target position, next simulation time, and global route node id. Do not store `Entity` or Unity view references.</action>
  <verify>Inspect contracts and confirm no `Entity`, GameObject, MonoBehaviour, View, or presentation fields are present.</verify>
  <done>AI navigation mode and far simulation state can exist without client view or loaded chunk.</done>
</task>

<task type="auto">
  <name>Task 2: Implement approximate far movement</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/FarAiApproximateMoveSystem.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Domain/FarAiMovementRules.cs</files>
  <action>Add a bounded server system that advances `AiFarSimulationState.LogicalPosition` toward `TargetPosition` on sparse ticks. Use explicit rules for speed/cost approximation. It may update navigation-owned logical state only; it must not mutate `CharacterNetState` directly if local bot movement owns that projection. Later handoff will reconcile through `AiMoveRequest` or owner-specific events.</action>
  <verify>Search `FarAiApproximateMoveSystem` for `Mut<CharacterNetState>`; it must not exist.</verify>
  <done>Far AI can progress logically without local nav mesh or client view.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Far movement does not require loaded chunk or active View.
- [ ] Far movement does not mutate local movement replication state directly.
- [ ] AI mode state is server-authoritative.
- [ ] Ask the user to run Unity compile checks if MCP validation is unavailable.
</verification>

<success_criteria>
- Far simulation is represented separately from local ProjectDawn movement.
- No presentation or transport concerns leak into navigation logic.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-navigation-modes/04-01-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 04-02-PLAN.md`.
</output>

