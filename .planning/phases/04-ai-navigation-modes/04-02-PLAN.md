---
phase: 04-ai-navigation-modes
type: execute
domain: unity-static-ecs
---

<objective>
Connect far-to-local handoff to the existing local movement path.

Purpose: when a chunk/local nav area becomes available, AI should resume movement through `AiMoveRequest` and `ServerAiNavigationSystem`, not by bypassing the existing ProjectDawn backend boundary.
Output: handoff system that converts navigation mode changes into movement intent.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/04-ai-navigation-modes/04-01-SUMMARY.md
@Assets/Scripts/StaticMlp/Features/AiBots/Runtime/Components/AiMoveRequest.cs
@Assets/Scripts/StaticMlp/Features/AiBots/Runtime/Systems/Server/ServerAiNavigationSystem.cs
@Assets/Scripts/StaticMlp/Features/AiBots/Runtime/Navigation/AiNavigation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add local attach/readiness contract</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/LocalNavAttachState.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/LocalNavAttachStateSystem.cs</files>
  <action>Add a navigation-owned state that records whether an entity can use local nav, using nav area/chunk availability facts discovered in the project. Keep chunk streaming implementation details outside `CombatDirector`. Do not store Unity ECS backend entities or GameObjects.</action>
  <verify>Inspect state fields and confirm they are stable data values, not engine object references.</verify>
  <done>Systems can tell when far AI is eligible to hand off into local navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Create handoff system through AiMoveRequest</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/FarToLocalAiNavigationHandoffSystem.cs, Assets/Scripts/StaticMlp/Features/AiBots/Runtime/Components/AiMoveRequest.cs</files>
  <action>Add a server system that, when local nav becomes available, writes or updates `AiMoveRequest` on the AI entity with the far simulation target/destination. This is an intentional write into the existing AiBots-owned movement intent only if `AiMoveRequest` is treated as the shared movement contract; otherwise stop and convert it to a contracts assembly first. Do not mutate `CharacterNetState` from the handoff system.</action>
  <verify>Search handoff system for `Mut<CharacterNetState>` and ProjectDawn APIs; neither should exist.</verify>
  <done>Far-to-local handoff routes through `AiMoveRequest`, and `ServerAiNavigationSystem` remains the only local movement application system.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Handoff does not call ProjectDawn or Unity.Entities backend APIs.
- [ ] Handoff does not directly change replicated character movement state.
- [ ] If `AiMoveRequest` ownership blocks cross-feature writes, execution stops and creates a contract extraction plan instead of silently violating ownership.
- [ ] Ask the user to run Unity compile checks if MCP validation is unavailable.
</verification>

<success_criteria>
- Local movement continues through the current `AiMoveRequest` -> `ServerAiNavigationSystem` -> `CharacterNetState` flow.
- Cross-feature ownership is explicitly respected.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-navigation-modes/04-02-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 05-01-PLAN.md`.
</output>

