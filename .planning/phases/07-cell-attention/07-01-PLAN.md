---
phase: 07-cell-attention
type: execute
domain: unity-static-ecs
---

<objective>
Replace passive wave pressure with reason-based cell attention.

Purpose: stop the world from becoming more dangerous just because a player exists in a cell.
Output: `CellAttention` contracts/systems/tests that accumulate only explicit causes and decay back toward calm.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/06-combat-director-openworld-audit/06-01-SUMMARY.md
@ai/gdd/AiCombatDirector_OpenWorld_Plan.md
@Assets/Scripts/StaticMlp/Features/CombatDirector/AGENTS.md
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Components/ThreatBudget.cs
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/ThreatBudgetAccumulationSystem.cs
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/PlayerThreatInputSystem.cs
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/WorldResources/EncounterDirectorConfig.cs
@Assets/Tests/Editor/CombatDirector/CombatDirectorRuntimeSystemsTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CellAttention contract and config fields</name>
  <files>Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Components/CellAttention.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/WorldResources/EncounterDirectorConfig.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/CombatDirectorGameplayFeature.cs</files>
  <action>Add `CellAttention` with `Current`, `Max`, `DecayPerSecond`, and explicit breakdown fields for noise, trespass, combat, loot, and faction alarm. Seed it on the director entity where `CombatCellTrackingSystem` currently seeds `ThreatBudget`. Keep the old `ThreatBudget` only if needed temporarily during migration, and mark any temporary compatibility with `[Obsolete("Temp")]`. Do not add lazy `Ensure*` helpers that hide missing director state.</action>
  <verify>Search confirms `CellAttention` is registered through normal world type registration and the director entity has it after `CombatCellTrackingSystem.Update()`.</verify>
  <done>Director cell has `CellAttention` with explicit cause fields and no hidden fallback creation path.</done>
</task>

<task type="auto">
  <name>Task 2: Replace passive accumulation with reason-based input and decay</name>
  <files>Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/PlayerThreatInputSystem.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/CellAttentionInputSystem.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/CellAttentionDecaySystem.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/CombatDirectorGameplayFeature.cs</files>
  <action>Move the runtime pressure calculation away from `BaseThreatPerSecond` and into explicit attention input. Passive presence in a cell must add zero dangerous pressure. Noise, attack/combat activity, carried loot, trespass, and future faction alarm inputs should map into named attention fields. Add a decay system that subtracts `DecayPerSecond * dt` and clamps at zero. Do not make attention permanent faction memory; long-term faction memory is a separate future feature.</action>
  <verify>Existing tests for threat accumulation are updated or replaced so player standing/walking without causes does not increase `CellAttention.Current`, while noise/loot/combat causes do.</verify>
  <done>Attention grows only from explicit causes and decays after causes stop.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests around attention behavior</name>
  <files>Assets/Tests/Editor/CombatDirector/CombatDirectorRuntimeSystemsTests.cs, Assets/Tests/Editor/CombatDirector/CombatDirectorContractsTests.cs</files>
  <action>Replace wave-budget assumptions with attention-specific tests: passive exploration stays calm, explicit cause fields contribute to `Current`, clamp prevents overflow, decay reduces attention, and deterministic identical input produces identical attention. Keep tests focused on contracts and systems touched by this phase.</action>
  <verify>Ask the user to run Unity editor tests for `Assets/Tests/Editor/CombatDirector` because agents must not run `dotnet build` and Unity compile is the authoritative check.</verify>
  <done>Tests describe reason-based attention instead of always-growing threat.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `ThreatBudgetAccumulationSystem` is removed, renamed, or marked temporary only if still needed for later phase compatibility.
- [ ] Passive player presence does not increase dangerous pressure.
- [ ] Attention decay has focused test coverage.
- [ ] No navigation backend, UI, or transport logic is added.
- [ ] Ask the user to run Unity compile/editor tests.
</verification>

<success_criteria>
- The director no longer relies on passive threat growth as the default open-world pressure source.
- Attention fields explain why pressure exists.
- Phase 08 can drive phases from `CellAttention` rather than old budget thresholds.
</success_criteria>

<output>
After completion, create `.planning/phases/07-cell-attention/07-01-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 08-01-PLAN.md`.
</output>
