---
phase: 05-debug-verification
type: execute
domain: unity-static-ecs
---

<objective>
Add debug visibility and acceptance verification for AiNavigation.

Purpose: make the CombatDirector/AiNavigation boundary observable without letting debug tools mutate gameplay or backend state.
Output: read-only debug snapshot contracts and presentation-only debug visualization path.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/04-ai-navigation-modes/04-02-SUMMARY.md
@Assets/Scripts/StaticMlp/Features/AiBots/Runtime/Presentation/BotNavigationGizmoPart.cs
@Assets/Scripts/StaticMlp/Features/AiBots/Runtime/Navigation/AiNavigation.md
@ai/CombatDirector_AiNavigation_Contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add read-only debug snapshot state</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/AiNavigationDebugSnapshot.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/AiNavigationDebugSnapshotSystem.cs</files>
  <action>Add debug snapshot state that reports active combat cell, active nav area, nav build state, nav version, reachable/unreachable source counts, selected/resolved source facts if available, phase name/id read from director state if exposed, technical budget counters, alive enemy count if exposed, path requests this tick, and reachability checks this tick. The system is read-only with respect to director-owned state and writes only navigation-owned debug snapshot data.</action>
  <verify>Search debug system for mutations to director state, threat budget, spawn source state, and backend entities; none should exist except navigation-owned debug state writes.</verify>
  <done>Debug data is inspectable as ECS state without changing gameplay behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Connect presentation-only debug visualization</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Presentation/AiNavigationDebugGizmoPart.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Presentation/StaticMlp.Features.AiNavigation.Presentation.asmdef</files>
  <action>Add a presentation assembly and passive gizmo/view part only if the project has an existing debug visualization path to attach it. It may read `AiNavigationDebugSnapshot` and existing `AiNavigationRuntime.TryGetDebugState` output but must not issue movement commands, mutate backend state, search scene hierarchy, or create prefabs. If manual inspector wiring is needed, document it in the summary instead of generating prefab assets.</action>
  <verify>Inspect presentation code and confirm it contains no calls to movement command APIs, backend mutation APIs, scene search helpers, or prefab asset creation.</verify>
  <done>Debug visualization is read-only and manually wireable by a human in Unity.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `CombatDirector` does not depend on `UnityEngine.AI`.
- [ ] `CombatDirector` does not call `NavMeshBuilder`.
- [ ] `AiNavigation` does not change director phase or threat budget.
- [ ] Spawn source selection uses cached reachable sources.
- [ ] `Peak` does not start mass nav rebuilds.
- [ ] Far AI can move without loaded chunk and hands off to local movement through `AiMoveRequest`.
- [ ] Client/presentation code is view/debug only.
- [ ] Ask the user to run Unity compile/play checks for final verification.
</verification>

<success_criteria>
- Required debug overlay facts are available as read-only state.
- The full `CombatDirector_AiNavigation_Contract.md` acceptance checklist is represented by automated checks or explicit manual Unity verification.
</success_criteria>

<output>
After completion, create `.planning/phases/05-debug-verification/05-01-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `AiNavigation v1 complete; plan follow-up hardening from discovered issues`.
</output>

