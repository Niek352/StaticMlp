---
phase: 09-spawn-source-classification
type: execute
domain: unity-static-ecs
---

<objective>
Classify spawn sources by world role and allowed director usage.

Purpose: make spawn requests come from believable world sources instead of treating every placement as a generic wave source.
Output: source kind/flags contracts, placement mapping updates, and tests that separate ambient, escalation, and pressure-event sources.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/08-openworld-phase-and-encounters/08-01-SUMMARY.md
@ai/gdd/AiCombatDirector_OpenWorld_Plan.md
@ai/CombatDirector_AiNavigation_Contract.md
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Components/SpawnSource.cs
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Definitions/SpawnSourceType.cs
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Domain/SpawnSourcePlacementRules.cs
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/SpawnSourcePlacementSeedSystem.cs
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/SpawnSourceSelectionSystem.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend source contracts with open-world classification</name>
  <files>Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Components/SpawnSource.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Definitions/SpawnSourceKind.cs</files>
  <action>Add `SpawnSourceKind` values for `AmbientPoint`, `PatrolRoute`, `CampGate`, `LairEntrance`, `Rift`, `RoadAmbush`, and `BaseRaidEntry`. Extend `SpawnSource` with kind, faction id, biome id, and explicit boolean flags `AllowsAmbient`, `AllowsEscalation`, and `AllowsPressureEvent`. Keep existing fields needed by AiNavigation reachability. Avoid creating a separate parallel source component unless the audit proves the existing contract cannot migrate safely.</action>
  <verify>Contract tests verify default/source initialization is explicit and no source can imply pressure-event use without `AllowsPressureEvent`.</verify>
  <done>Source usage is explicit in data instead of inferred from generic source type.</done>
</task>

<task type="auto">
  <name>Task 2: Update placement mapping and selection rules</name>
  <files>Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Domain/SpawnSourcePlacementRules.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/SpawnSourcePlacementSeedSystem.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/SpawnSourceSelectionSystem.cs</files>
  <action>Map existing open-world spawn placement kinds to explicit source kinds and flags. Unknown placement kinds remain integration errors and must fail fast. Selection must filter by requested source usage: ambient source selection cannot return pressure-only sources, and future pressure selection cannot use ambient-only sources. Do not add fallback remapping to `Rift`/`Burrow` for unknown kinds.</action>
  <verify>Tests cover active/inactive sources, distance validation, and usage flags for ambient versus pressure source selection.</verify>
  <done>Spawn source selection respects world role and allowed usage.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve AiNavigation boundary</name>
  <files>Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/ReachableSpawnSourceCacheSystem.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Logic/Systems/Server/ResolvedSpawnPointSystem.cs, Assets/Scripts/StaticMlp/Features/AiNavigation/Runtime/Contracts/ReachableSpawnSourceCandidate.cs</files>
  <action>Adjust AiNavigation consumers only as needed for the changed `SpawnSource` contract. AiNavigation may read source position, radius, active state, and navigation-relevant classification, but it must not decide encounter kind, phase, escalation, or pressure composition. If no code changes are needed, document that in the summary.</action>
  <verify>`rg -n "DirectorPhase|CellAttention|EncounterState|EnemyRole" Assets/Scripts/StaticMlp/Features/AiNavigation` shows AiNavigation is not choosing director drama.</verify>
  <done>Navigation reachability still reads source facts without owning combat decisions.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Spawn source flags are explicit and tested.
- [ ] Unknown placement kinds still fail fast.
- [ ] Ambient-only sources cannot be selected for pressure use.
- [ ] AiNavigation does not mutate or decide director gameplay state.
- [ ] Ask the user to run Unity compile/editor tests.
</verification>

<success_criteria>
- The director can distinguish ambient, escalation, and pressure source roles.
- Source classification remains compatible with cached AiNavigation reachability.
- Phase 10 can build ambient requests from ambient-enabled sources only.
</success_criteria>

<output>
After completion, create `.planning/phases/09-spawn-source-classification/09-01-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 10-01-PLAN.md`.
</output>
