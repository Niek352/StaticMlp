---
phase: 06-combat-director-openworld-audit
type: execute
domain: unity-static-ecs
---

<objective>
Audit the existing `CombatDirector` implementation before refactoring it toward the open-world attention model.

Purpose: avoid replacing working architecture blindly and identify the exact runtime, contract, test, and documentation changes needed for phases 07-10.
Output: an audit note that maps current files to the open-world plan and lists architecture conflicts.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@ai/gdd/AiCombatDirector_OpenWorld_Plan.md
@ai/combat_director.md
@ai/CombatDirector_AiNavigation_Contract.md
@Assets/Scripts/StaticMlp/Features/CombatDirector/AGENTS.md
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/CombatDirectorGameplayFeature.cs
@Assets/Tests/Editor/CombatDirector/CombatDirectorRuntimeSystemsTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map existing CombatDirector runtime surface</name>
  <files>ai/gdd/AiCombatDirector_OpenWorld_Audit.md</files>
  <action>Create an audit document that lists existing `CombatDirector` contract, logic, presentation, event, world resource, catalog, and test files. Identify which files belong to current wave-first behavior and which files can remain unchanged during the open-world refactor. Do not write runtime code in this phase; this is an architecture audit.</action>
  <verify>`rg -n "ThreatBudget|DirectorPhase|SpawnSource|SpawnRequest|EnemySpawnApplySystem" Assets/Scripts/StaticMlp/Features/CombatDirector Assets/Tests/Editor/CombatDirector ai` was used to ground the file map.</verify>
  <done>Audit document includes existing files, their responsibility, and whether each is keep, refactor, replace, or defer.</done>
</task>

<task type="auto">
  <name>Task 2: Record open-world conflicts and safe migration order</name>
  <files>ai/gdd/AiCombatDirector_OpenWorld_Audit.md</files>
  <action>Add a section describing conflicts between the current wave-first flow and the open-world design. Cover `ThreatBudget`, `DirectorPhase`, source selection, spawn request build, alive caps, reachability, presentation, and tests. Recommend a safe migration order for phases 07-10. Do not propose fallback spawn positions or hidden compatibility wrappers; invalid architecture should fail fast or be explicitly deferred.</action>
  <verify>Audit references `ai/gdd/AiCombatDirector_OpenWorld_Plan.md`, `ai/combat_director.md`, and `ai/CombatDirector_AiNavigation_Contract.md`.</verify>
  <done>Conflicts and migration order are explicit enough for phase 07 to start without re-auditing.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `ai/gdd/AiCombatDirector_OpenWorld_Audit.md` exists.
- [ ] The audit identifies all current wave-first contracts and systems.
- [ ] No runtime `.cs` files are modified.
- [ ] The next step clearly points to `07-01-PLAN.md`.
</verification>

<success_criteria>
- Current `CombatDirector` architecture is mapped before implementation.
- Refactor risks are documented instead of discovered mid-change.
- Phase 07 has a concrete, file-level starting point.
</success_criteria>

<output>
After completion, create `.planning/phases/06-combat-director-openworld-audit/06-01-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 07-01-PLAN.md`.
</output>
