---
phase: 01-core-combat-and-gathering
plan: 01-02
type: execute
domain: unity-static-ecs-networked-gameplay
---

<objective>
Wire active target selection and server combat validation to the target contracts from Plan 1.

Purpose: The player must be able to move and aim at enemies or resource nodes without entering a separate gather mode.
Output: Client target selection for actor/resource targets and server validation that emits combat hit facts without mutating foreign feature state.
</objective>

<execution_context>
@.agents/skills/create-plans/workflows/execute-phase.md
@.agents/skills/create-plans/templates/summary.md
@.agents/skills/staticmlp-code-writing/SKILL.md
@.agents/skills/staticmlp-networked-feature/SKILL.md
</execution_context>

<context>
@AGENTS.md
@Assets/Scripts/StaticMlp/Features/AGENTS.md
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-core-combat-and-gathering/01-01-SUMMARY.md
@Assets/Scripts/StaticMlp/Features/Player/Runtime/Systems/Client/LocalPlayerMovementSystem.cs
@Assets/Scripts/StaticMlp/Game/Input/ClientInputState.cs
@Assets/Scripts/StaticMlp/Game/Components/CharacterNetState.cs
@Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Client/ClientPassiveAutoAttackTargetingSystem.cs
@Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Client/ClientPassiveAutoAttackIntentSystem.cs
@Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Client/ClientPassiveAutoAttackSendSystem.cs
@Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Server/ServerReceiveCombatCommandsSystem.cs
@Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Server/ServerValidateCombatCommandsSystem.cs
@Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Server/ServerAbilityCastSystem.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select active aim targets on the client</name>
  <files>Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Client/ClientPassiveAutoAttackTargetingSystem.cs, Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/StaticMlp.Features.Combat.Logic.asmdef</files>
  <action>Update client targeting to prefer the player's aim ray when available via `ClientInputState.TryGetAimRay`. Evaluate actor targets from `MonsterTag + CharacterNetState` and resource targets from OpenWorldResources target contracts added in Plan 1. Use range, aim cone/screen proximity, alive/depleted state, and deterministic tie-breakers. Fall back to nearest target only when there is no valid aim-selected target. Store the result as `CombatTargetRef` in `PassiveAutoAttackState`. Reference only OpenWorldResources.Contracts, not Presentation.</action>
  <verify>`rg -n "TryGetAimRay|CombatTargetRef|OpenWorldResourceTarget" Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Client/ClientPassiveAutoAttackTargetingSystem.cs` shows active aim and both target families are handled.</verify>
  <done>Local player target selection can choose either an enemy actor or a resource placement while movement remains uninterrupted.</done>
</task>

<task type="auto">
  <name>Task 2: Send ability commands with target refs</name>
  <files>Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Client/ClientPassiveAutoAttackIntentSystem.cs, Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Client/ClientPassiveAutoAttackSendSystem.cs</files>
  <action>Update intent creation and command sending to use `CombatTargetRef`. Keep shot sequence/cooldown behavior unchanged. Do not send resource-specific commands from the client directly to OpenWorldResources; all attack intent goes through Combat's server-validated ability command.</action>
  <verify>`rg -n "new UseAbilityCommand|PassiveAutoAttackIntent|CombatTargetRef" Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Client` shows command creation uses target refs and still increments shot sequence once per accepted local fire time.</verify>
  <done>Client attack intent uses one command path for enemy damage and resource hits.</done>
</task>

<task type="auto">
  <name>Task 3: Validate actor and static-placement combat requests on the server</name>
  <files>Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Server/ServerReceiveCombatCommandsSystem.cs, Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Server/ServerValidateCombatCommandsSystem.cs, Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Server/ServerAbilityCastSystem.cs</files>
  <action>Update server receive/validation for `CombatTargetRef`. Actor targets must preserve existing checks: source exists, prepared ability, target has `CharacterNetState` and `Health`, target alive, range, cooldown, shot sequence. Static placement targets must validate only Combat-owned facts here: source exists, prepared ability, cooldown, shot sequence, finite quantized hit point, and non-zero placement id. Placement existence, depleted state, and range to the authoritative resource position are OpenWorldResources-owned checks and must be performed in Plan 3 before overlay mutation. `ServerAbilityCastSystem` should emit a `CombatTargetHitEvent` for valid static-placement requests; it should continue creating `CombatHit` only for actor targets. Do not mutate OpenWorldResources overlay here.</action>
  <verify>`rg -n "CombatTargetHitEvent|CombatTargetKind|CombatHit" Assets/Scripts/StaticMlp/Features/Combat/Runtime/Logic/Systems/Server` shows static-placement requests emit a combat event and actor requests continue through the damage pipeline.</verify>
  <done>Server accepts one validated combat command path and emits owner-consumable hit facts for resource placements.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] Existing actor combat still routes through `CombatHit`, `ServerHitToEffectSystem`, and `ServerDamageApplySystem`.
- [ ] Static-placement combat emits `CombatTargetHitEvent` and does not write OpenWorldResources components/resources.
- [ ] Invalid client commands are rejected as gameplay validation failures, not hidden by fallback targeting.
- [ ] If replicated command fields changed, ask the user to run the Unity replication/codegen workflow.
- [ ] Ask the user to run Unity compile checks; do not run `dotnet build`.
</verification>

<success_criteria>
- Player movement and active aim can coexist.
- Enemies and resource placements are both targetable by client combat logic.
- Server validation remains authoritative for cooldown, actor range, ability, and duplicate command rejection; OpenWorldResources owns static placement/range validation in the next plan.
- Cross-feature mutation boundary is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-combat-and-gathering/01-02-SUMMARY.md` and update `.planning/phases/01-core-combat-and-gathering/01-PHASE-SUMMARY.md`.

# Phase 1 Plan 2: Active Targeting Summary

**[Substantive one-liner]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Deviations from Plan
## Issues Encountered
## Next Step
Ready for `01-03-PLAN.md`.
</output>
