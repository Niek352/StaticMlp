---
phase: 08-openworld-phase-and-encounters
type: execute
domain: unity-static-ecs
---

<objective>
Introduce open-world director phases and encounter state so small encounters can resolve without becoming waves.

Purpose: replace `Calm -> BuildUp -> Peak -> Relief -> Cooldown` with a conservative open-world phase machine.
Output: phase/encounter contracts, phase system updates, and tests for non-escalating encounters.
</objective>

<execution_context>
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/workflows/execute-phase.md
@C:/Users/pavel/_UnityProjects/StaticMlp/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/07-cell-attention/07-01-SUMMARY.md
@ai/gdd/AiCombatDirector_OpenWorld_Plan.md
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Definitions/DirectorPhase.cs
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Components/DirectorState.cs
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/DirectorPhaseSystem.cs
@Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/SpawnRequestBuildSystem.cs
@Assets/Tests/Editor/CombatDirector/CombatDirectorRuntimeSystemsTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace wave-first phase semantics</name>
  <files>Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Definitions/DirectorPhase.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Components/DirectorState.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/DirectorPhaseSystem.cs</files>
  <action>Change director phases to `Dormant`, `Ambient`, `Contact`, `Suspicion`, `Escalation`, `PressureEvent`, `Recovery`, and `Cooldown`, preserving stable replicated serialization behavior in `DirectorState`. Phase transitions must be conservative: `Ambient` stays ambient without contact/reason, `Contact` does not auto-escalate, `Suspicion` can decay to `Recovery`, and `PressureEvent` is not reachable without explicit eligibility. Do not keep old `Peak` behavior as the default under a new name.</action>
  <verify>Tests prove passive exploration remains `Ambient` and high attention alone cannot create pressure without the required phase/reason path.</verify>
  <done>Old wave-first phase names no longer drive default spawn behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add encounter state contracts and lifetime skeleton</name>
  <files>Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Components/EncounterState.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Definitions/EncounterKind.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Contracts/Definitions/EncounterIntensity.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/EncounterContactDetectionSystem.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/EncounterLifetimeSystem.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/EncounterRecoverySystem.cs, Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/CombatDirectorGameplayFeature.cs</files>
  <action>Add `EncounterState`, `EncounterKind`, and `EncounterIntensity` so solo/small encounters have explicit runtime state. Implement minimal server systems for contact detection, time/alive tracking, and recovery using current available inputs only. If required camp/lair/territory facts do not exist yet, document them as deferred inputs instead of creating fake helper systems or fallback facts.</action>
  <verify>Tests can create a solo encounter, advance lifetime, and resolve it without creating a spawn request.</verify>
  <done>Encounter state exists independently from pressure events and can complete without a wave.</done>
</task>

<task type="auto">
  <name>Task 3: Block old spawn request path outside pressure phases</name>
  <files>Assets/Scripts/StaticMlp/Features/CombatDirector/Runtime/Logic/Systems/SpawnRequestBuildSystem.cs, Assets/Tests/Editor/CombatDirector/CombatDirectorRuntimeSystemsTests.cs</files>
  <action>Update request building so ordinary `Ambient`, `Contact`, and `Recovery` states do not produce wave-like spawn requests. If the old builder must remain for later pressure-event work, gate it behind `DirectorPhase.PressureEvent` or an explicitly temporary compatibility path marked `[Obsolete("Temp")]`. Do not silently translate every old `BuildUp`/`Peak` case to the new model.</action>
  <verify>Tests prove a contact encounter with low or moderate attention does not create swarm requests.</verify>
  <done>Waves are not the default response to encounter state.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Replicated `DirectorState` still serializes phase values deterministically.
- [ ] Ambient/contact encounter tests do not produce wave requests.
- [ ] Any temporary old wave compatibility is explicitly marked `[Obsolete("Temp")]`.
- [ ] Ask the user to run Unity compile/editor tests.
</verification>

<success_criteria>
- Open-world phases are represented in code and tests.
- Small encounters can start and finish without escalation.
- Phase 09 can classify sources without fighting old phase semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/08-openworld-phase-and-encounters/08-01-SUMMARY.md` with accomplishments, files changed, decisions made, issues encountered, and next step `Ready for 09-01-PLAN.md`.
</output>
